Respond with the Windows build command for this project:

```bash
zig build windows
```

This command cross-compiles a Windows executable and copies it to `D:\Projects\ZigGame\zig_game.exe`.

Just output the command, no other explanation needed.
